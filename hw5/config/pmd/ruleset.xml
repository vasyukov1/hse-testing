<?xml version="1.0"?>
<ruleset name="hw5-ruleset"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
                        https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>Standard ruleset for hw5 project</description>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <!-- allow System.err.println in legacy code -->
        <exclude name="SystemPrintln"/>
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="AtLeastOneConstructor"/>
        <exclude name="OnlyOneReturn"/>
        <exclude name="CommentDefaultAccessModifier"/>
        <exclude name="LocalVariableCouldBeFinal"/>
        <exclude name="MethodArgumentCouldBeFinal"/>
        <exclude name="LongVariable"/>
        <exclude name="ShortVariable"/>
        <exclude name="TooManyStaticImports"/>
        <exclude name="UnnecessaryBoxing"/>
        <!-- generated Gradle wrapper code – huge method -->
        <exclude name="AvoidDollarSigns"/>
    </rule>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter"/>
        <exclude name="LoosePackageCoupling"/>
        <exclude name="CyclomaticComplexity"/>
        <exclude name="GodClass"/>
        <exclude name="SingularField"/>
        <!-- Storage class intentionally large -->
        <exclude name="TooManyMethods"/>
        <exclude name="ExcessivePublicCount"/>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <!-- BCrypt hash comparison via .equals() is intentional in some places -->
        <exclude name="UseEqualsToCompareStrings"/>
        <exclude name="NullAssignment"/>
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml">
        <exclude name="UseConcurrentHashMap"/>
        <exclude name="DoNotUseThreads"/>
        <exclude name="AvoidUsingVolatile"/>
        <exclude name="AvoidSynchronizedAtMethodLevel"/>
    </rule>

    <!-- Performance -->
    <rule ref="category/java/performance.xml"/>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>

</ruleset>
